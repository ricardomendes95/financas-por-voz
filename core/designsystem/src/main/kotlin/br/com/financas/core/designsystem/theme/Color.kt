package br.com.financas.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// Cores semânticas — carregam significado, nunca decoração (§10.1).
val IncomeLight = Color(0xFF16A34A)
val IncomeDark = Color(0xFF4ADE80)
val ExpenseLight = Color(0xFFDC2626)
val ExpenseDark = Color(0xFFF87171)
val WarningLight = Color(0xFFD97706)
val WarningDark = Color(0xFFFBBF24)

// Superfícies do dark mode — dark de verdade, não cinza lavado (§10.3).
val DarkSurfaceBase = Color(0xFF0F1115)
val DarkSurfaceElevated = Color(0xFF1A1D23)
